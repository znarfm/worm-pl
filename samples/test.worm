def main() {
    # This is a single line comment
    """
    This is a multi-line
    comment that spans
    several lines
    """
    x:int = 42;  # Assigning a number
    if (x > 0) {
        print("Positive");
    };
    print("Done");
}